rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ── Helper de Autenticación ───────────────────────────────────────────────
    function isAuth() {
      return request.auth != null;
    }

    // ── Configuración global y enlaces compartidos (acceso público) ──────────
    match /config/{docId} {
      allow read, write: if true;
    }

    match /sharedPlans/{planId} {
      allow read, create, update: if true;
      allow delete: if isAuth();
    }

    match /sharedSessions/{shareId} {
      allow read, create, update: if true;
      allow delete: if isAuth();
    }

    match /team_codes/{codeId} {
      allow read: if true;
      allow write: if isAuth();
    }

    match /staff_invitations/{invitationId} {
      allow read: if true;
      allow create, update, delete: if isAuth();
    }

    // ── Acceso Total a todas las Colecciones y Subcolecciones para Usuarios Autenticados ──
    match /{document=**} {
      allow read, write: if isAuth();
    }

  }
}
